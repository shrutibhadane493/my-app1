pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo "Build stage running..."
            }
        }

        stage('Test') {
            steps {
                echo "Test stage running..."
            }
        }

        stage('Deploy') {
            steps {
                echo "Deployment done."
            }
        }
    }
}
